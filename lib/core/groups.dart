class Groups {
  Groups({
    required this.name,
    required this.photo,
    required this.lastSeen,
    required this.lastMessage,
  });

  String name;
  String photo;
  DateTime lastSeen;
  String lastMessage;
}
